services:
  drupalup_create_content.commands:
    class: \Drupal\drupalup_create_content\Commands\DrupalupCreateContentCommand
    arguments: ['@entity_type.manager']
    tags:
      - { name: drush.command }
